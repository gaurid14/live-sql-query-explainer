<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.gauri.sqlqueryexplainer</id>
    <name>Live SQL Query Explainer</name>
    <vendor email="gauridesai35@gmail.com" url="https://github.com/gaurid14">Gauri Desai</vendor>

    <description>
        <![CDATA[
            <p>
            <strong>Live SQL Query Explainer</strong> helps developers understand SQL queries inline, directly within IntelliJ-based IDEs.
            </p>
            <p><strong>Key features include:</strong></p>
            <ul>
              <li><strong>Inline SQL explanation</strong> in plain English with helpful visual cues</li>
              <li>Support for common SQL operations: <code>SELECT</code>, <code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code></li>
              <li>Detailed breakdowns of SQL clauses like <code>JOIN</code>, <code>WHERE</code>, <code>GROUP BY</code>, <code>ORDER BY</code>, and <code>LIMIT</code></li>
              <li>A dedicated <strong>tool window</strong> to view and experiment with built-in sample queries</li>
              <li>Sample queries for all supported SQL types to help you learn or debug easily</li>
            </ul>
            <p>
            Ideal for beginners learning SQL and professionals debugging complex queries.
            </p>
            <p>
            🔗 <a href="https://github.com/gaurid14/live-sql-query-explainer">GitHub Repository</a>
            </p>
        ]]>
    </description>

    <depends>com.intellij.modules.platform</depends>

    <resource-bundle>messages.MyBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="SQL Explainer"
                    factoryClass="com.gauri.sqlqueryexplainer.SQLToolWindowFactory"
                    anchor="right"
                    canCloseContents="false"
                    icon="/icons/sql.png"/>
    </extensions>

    <change-notes>
        Added support for INSERT, UPDATE, and DELETE SQL statements.

        Sample queries for INSERT, UPDATE, and DELETE added to the tool window for easier testing and learning.

    </change-notes>

    <actions>
        <action id="SQLQueryExplainAction"
                class="com.gauri.sqlqueryexplainer.SQLExplainAction"
                text="Explain SQL Query"
                description="Analyze and explain SQL queries inline. Supports SELECT, INSERT, UPDATE, DELETE, JOIN, GROUP BY, ORDER BY, and more."
                icon="/icons/sql.png">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
